# FastAPI stack
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
httpx>=0.26.0
websockets>=12.0

# HTTP client (still needed for sync operations in scheduler)
requests>=2.31.0

# Background job scheduling
APScheduler>=3.10.4

# AWS
boto3>=1.34.0

# Email validation
email-validator>=2.0.0

# Environment variables
python-dotenv>=1.0.0

# Webflow CMS management (install from local path or git)
# pip install -e /path/to/FillWebflowFields
webflow-cms>=1.0.0
